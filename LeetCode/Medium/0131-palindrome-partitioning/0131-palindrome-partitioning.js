/**
 * @param {string} s
 * @return {string[][]}
 */
var partition = function (s) {

};

const isPalindrome = (s: string): boolean => {
    for(let i = 0; i < s.length; i++) {
        
    }

    return false;
}